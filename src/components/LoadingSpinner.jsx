import { HashLoader } from "react-spinners";

const LoadingSpinner = () => {
  return (
    <div className="h-[100vh] flex justify-center items-center">
      <HashLoader color="#2C4263" />
    </div>
  );
};

export default LoadingSpinner;
